# Copy to .env and fill in:  cp .env.example .env
# This file holds the token file *path*, never the token itself — keep the
# actual token in the file TOKEN_FILE_HOST points to (chmod 600).
GOOGLE_EMAIL=you@example.com
TOKEN_FILE_HOST=/home/you/.config/keep-mcp/google_master_token
# UNSAFE_MODE=false
